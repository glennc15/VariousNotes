# Linux commands

```
# update the distro:
sudo apt update
sudo apt full-upgrade -y

# install Chrome:

# install GitHub CLI
sudo apt install gh
gh auth login

# create a new repository on the command line
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/glennc15/VariousNotes.git
git push -u origin main

# push an existing repository from the command line
git remote add origin https://github.com/glennc15/VariousNotes.git
git branch -M main
git push -u origin main
```

